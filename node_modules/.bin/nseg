#!/bin/sh
basedir=`dirname "$0"`

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir//bin/bash" ]; then
  "$basedir//bin/bash"  "$basedir/../nseg/bin/nseg" "$@"
  ret=$?
else 
  /bin/bash  "$basedir/../nseg/bin/nseg" "$@"
  ret=$?
fi
exit $ret
